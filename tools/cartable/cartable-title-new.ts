import type { McpServer } from "@modelcontextprotocol/server";
import type { TToolConfig } from "../../types";

const name = "cartable-new-title";
const config: TToolConfig = {
    title: "Cartable Title New",
    description: "Create"
};

export default function cartableTitleNew(server: McpServer) {}
