{
    "mcpServers": {
        "portal-mcp": {
            "url": "http://localhost:8080/mcp",
            "envFile": "${workspaceFolder}/.env",
            "headers": {
                "Authorization": "Bearer ${env:PORTAL_MCP_TOKEN}"
            }
        }
    }
}
